# JWT_Authentication_React

# package.json

"build": "tsc",
"start": "node dist/app.js",
"dev": "ts-node-dev --respawn --transpile-only src/app.ts"

# tsconfig.json

"outDir": "./dist",
"rootDir": "./src"
